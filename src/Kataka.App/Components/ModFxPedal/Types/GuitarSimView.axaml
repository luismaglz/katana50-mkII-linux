<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Kataka.App.ViewModels"
             xmlns:dvm="using:Kataka.App.ViewModels.Design"
             xmlns:controls="using:Kataka.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{x:Static dvm:DesignModFxPedalViewModel.Instance}"
             x:Class="Kataka.App.Views.ModsFx.GuitarSimView"
             x:DataType="vm:ModFxPedalViewModel"
             x:CompileBindings="False">
    <StackPanel Spacing="8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <ComboBox ItemsSource="{Binding GuitarSimTypeOptions}"
                      SelectedItem="{Binding GuitarSimTypeOption}" />
            <controls:RotaryKnobV2 Scale="0.75" Label="LOW"
                                   Minimum="0" Maximum="100"
                                   Value="{Binding GuitarSimLow}"
                                   LabelFontSize="11" />
            <controls:RotaryKnobV2 Scale="0.75" Label="HIGH"
                                   Minimum="0" Maximum="100"
                                   Value="{Binding GuitarSimHigh}"
                                   LabelFontSize="11" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <controls:RotaryKnobV2 Scale="0.75" Label="LEVEL"
                                   Minimum="0" Maximum="100"
                                   Value="{Binding GuitarSimLevel}"
                                   LabelFontSize="11" />
            <controls:RotaryKnobV2 Scale="0.75" Label="BODY"
                                   Minimum="0" Maximum="100"
                                   Value="{Binding GuitarSimBody}"
                                   LabelFontSize="11" />
        </StackPanel>
    </StackPanel>
</UserControl>
