<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Kataka.App.ViewModels"
             xmlns:dvm="using:Kataka.App.ViewModels.Design"
             xmlns:controls="using:Kataka.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{x:Static dvm:DesignModFxPedalViewModel.Instance}"
             x:Class="Kataka.App.Views.ModsFx.WaveSynthView"
             x:DataType="vm:ModFxPedalViewModel"
             x:CompileBindings="False">
    <StackPanel Spacing="8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <ComboBox ItemsSource="{Binding WaveSynthWaveOptions}"
                      SelectedItem="{Binding WaveSynthWaveOption}" />
            <controls:RotaryKnob Scale="0.75" Label="CUTOFF"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthCutoff}"
                                 LabelFontSize="11" />
            <controls:RotaryKnob Scale="0.75" Label="RESONANCE"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthResonance}"
                                 LabelFontSize="11" />
            <controls:RotaryKnob Scale="0.75" Label="FILTER SENS"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthFilterSens}"
                                 LabelFontSize="11" />
            <controls:RotaryKnob Scale="0.75" Label="FILTER DECAY"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthFilterDecay}"
                                 LabelFontSize="11" />
            <controls:RotaryKnob Scale="0.75" Label="FILTER DEPTH"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthFilterDepth}"
                                 LabelFontSize="11" />
            <controls:RotaryKnob Scale="0.75" Label="SYNTH LEVEL"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthSynthLevel}"
                                 LabelFontSize="11" />
            <controls:RotaryKnob Scale="0.75" Label="DIRECT MIX"
                                 Minimum="0" Maximum="100"
                                 Value="{Binding WaveSynthDirectMix}"
                                 LabelFontSize="11" />
        </StackPanel>
    </StackPanel>
</UserControl>
